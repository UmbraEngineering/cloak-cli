
var Router = require('cloak/router');

var {{ name.class }}Router = module.exports = Router.extend({

	routes: {
		'/home':  'home'
	},

	initialize: function() {
		this.on('notfound', function() {
			// 
			// 
			// 
		});
	},

	home: function() {
		// 
		// 
		// 
	}

});
